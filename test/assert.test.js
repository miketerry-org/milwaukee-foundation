// assert.test.js:

"use strict";

// import all necessary modules
import assert from "../lib/assert.js";

describe(`assert.equal`, async () => {
  it(`should support assert.equal`, async () => {
    // assert.strictEqual(1, 1);
  });
});
